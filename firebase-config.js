// ---------------------------------------------------------------------------
// Firebase web config for project "emrtemplate".
//
// These values are safe to expose in client-side code (Firebase web API keys
// are identifiers, not secrets). Real security comes from your Realtime
// Database security rules — see database.rules.json.
//
// >>> REPLACE the placeholder values below with your real config. <<<
// Firebase console → Project settings (gear icon) → "Your apps" → Web app →
// "SDK setup and configuration" → Config. Then copy the values here.
//
// Direct link:
// https://console.firebase.google.com/project/emrtemplate/settings/general
// ---------------------------------------------------------------------------

export const firebaseConfig = {
  apiKey: "REPLACE_ME_API_KEY",
  authDomain: "emrtemplate.firebaseapp.com",
  // Realtime Database URL — usually one of:
  //   https://emrtemplate-default-rtdb.firebaseio.com          (US)
  //   https://emrtemplate-default-rtdb.<region>.firebasedatabase.app (other regions)
  databaseURL: "REPLACE_ME_DATABASE_URL",
  projectId: "emrtemplate",
  storageBucket: "emrtemplate.appspot.com",
  messagingSenderId: "REPLACE_ME_SENDER_ID",
  appId: "REPLACE_ME_APP_ID",
};
