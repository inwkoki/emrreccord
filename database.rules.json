{
  "rules": {
    "messages": {
      // Any signed-in user (anonymous auth counts) can read the chat history.
      ".read": "auth != null",
      // New messages can be appended by signed-in users, but not edited/deleted.
      ".write": "auth != null",
      ".indexOn": ["ts"],
      "$msgId": {
        // Validate the shape of each message.
        ".validate": "newData.hasChildren(['uid', 'name', 'text', 'ts']) && newData.child('uid').val() === auth.uid && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 500 && newData.child('name').isString() && newData.child('name').val().length <= 24",
        "uid":  { ".validate": "newData.isString()" },
        "name": { ".validate": "newData.isString()" },
        "text": { ".validate": "newData.isString()" },
        "ts":   { ".validate": "newData.val() <= now" },
        "$other": { ".validate": false }
      }
    }
  }
}
